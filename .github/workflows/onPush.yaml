name: Run tests
on: push

jobs:
  build:
    name: Run tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macOS-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run the tests
        run: |
          . ./cd-extras/public/_Classes.ps1
          md ./_reports
          Import-Module Pester
          cd tests
          ./test.ps1 -Cover -EnableExit -CodeCoverageOutputFile ../_reports/coverage.xml -OutputFile ../_reports/testresults.xml
        shell: pwsh

      # run the tests again on Windows, this time in PowerShell v5
      - name: Run the tests (Powershell v5)
        if: matrix.os == 'windows-latest'
        run: |
          . ./cd-extras/public/_Classes.ps1
          md ./_reports -ea Ignore
          Import-Module Pester
          ./test.ps1 -EnableExit -OutputFile ../_reports/testresults.xml
        shell: powershell

      - name: Upload test report
        uses: actions/upload-artifact@master
        with:
          name: Test Report (${{ matrix.os }})
          path: _reports

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: ./_reports/coverage.xml
          flags: ${{ runner.os }}
